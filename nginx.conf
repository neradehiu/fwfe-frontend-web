server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    index index.html;

    # Serve React app ở /react/
    location /react/ {
        try_files $uri /react/index.html;
    }

    # Optional: redirect root / to /react/
    # location / {
    #     return 301 /react/;
    # }
}
